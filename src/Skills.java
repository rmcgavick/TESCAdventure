
public class Skills {
	public int checkPriviledge(Greener op) {
		int priv = op.stats.getPriviledge();
		
		return (priv / 2);
	}

	public int whiteGuilt(Greener op) {
		if (op.getEth() == WHITE) {
			
		}
	}
}
